package de.hsw;

public class Starter {
    public static void main(String[] args) {
        // 1. Kopieren Sie die fertig erstellten Klassen aus 42-collexperiment in dieses Projekt
        // 2. Implementieren Sie java.io.Serializable in allen Klassen
        // 3. Implementieren Sie eine Klasse "Hochschule", die die anderen Objekte hält
        // 4. Implementieren Sie eine save/load Methode, die "Hochschule" speichern bzw. laden kann
        // 5. Implementieren Sie die main Methode so, dass "Hochschule" beim Start geladen und beim beenden gespeichert wird
        // Wenn (z.B. beim 1. Start) noch keine Datei zum laden vorhanden ist, soll dies nicht zum Fehler führen (try/catch)
    }
}
